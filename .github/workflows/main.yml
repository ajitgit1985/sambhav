name: Deploy to Self-Hosted Runner

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
    

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        
      

      - name: SonarCloud Scan
        uses: sonarsource/sonarqube-scan-action@master
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3          
        

      - name: Build Docker image
        run: |
          # Navigate to the application directory
          cd /home/sambhav/sambhav
          docker build -t devopsajit85/sambhav-new:latest .
      - name: Trivy image scan 
        run : truvy image --format table -o image.html devopsajit85/sambhav-new:latest         

          # Build the Docker containers from the Dockerfile(s)
          #docker-compose build

          # Optional: Start the containers
         # docker-compose down
        #  docker-compose up -d

